spring.application.name = eureka-server
server.port = 8761
eureka.client.register-with-eureka = false  
# makes sure eureka server will not register itself
eureka.client.fetch-registry = false
# caching of the services registered with the eureka server